#target photoshopvar docRef = app.activeDocument;function main() {	if ( app.documents.length <= 0) {		alert( "uiAlertDocumentMustBeOpened" );		return;	}}var count = 0;var numberOfCopies = 5;while( count < numberOfCopies) {    var newLayer = docRef.activeLayer.duplicate();    docRef.activeLayer = newLayer;    newLayer.textItem.contents = count;    newLayer.selected = true;    count++; }